# Local Jenkins Notes

- Ensure Appium server is running on the Jenkins agent (or reachable).
- For iOS on macOS agents, ensure Xcode and WebDriverAgent prerequisites are met.
- Provide device farm connection as needed (USB passthrough or cloud).

